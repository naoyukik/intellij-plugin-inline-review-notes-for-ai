# Issue 44: 編集時のコメント入力パネル幅を新規作成時と統一する 実装計画

## Phase 0: 調査と詳細設計

- [x] Task: 既存のコメント入力パネルとポップアップ生成経路を調査する
  - [x] `autonomous-researcher` を用いて関連する実装、テスト、IntelliJ Platform API を調査する。
  - [x] 編集時と新規作成時の初期幅設定、`existingCommentId` の分岐、エディタペイン幅の取得箇所を特定する。
  - [x] 調査結果を `evidence_report.md` に記録する。
- [x] Task: 実装計画を具体化する
  - [x] 調査結果に基づき、Phase 1 の対象ファイル、行範囲、テストケース、最小変更方針を追記する。
- [x] Task: 環境と検証コマンドを確認する
  - [x] Gradle のテストおよび静的解析タスクの利用可否を確認する。
- [x] Task: Conductor - ユーザー手動検証
  - [x] 調査結果と詳細化した実装計画の妥当性についてユーザー承認を得る。
- [x] Task: Phase Verification & Checkpoint
  - [x] `./gradlew test` と利用可能な静的解析を実行し、結果を確認する。
  - [x] ユーザーの明示的承認後、当該フェーズで未コミットの変更がある場合にのみチェックポイントコミットを作成する。
  - [x] チェックポイントコミットを作成した場合のみ、その SHA を計画へ記録し、計画更新を別コミットにする。
  - チェックポイントコミット: `5a26b59`

## Phase 1: 編集時の初期幅を統一する

- [ ] Task: 編集時の初期幅を定義する失敗テストを追加する
  - [ ] `CommentInputPanelTest.kt:216-301` の軽量テストで、既存コメントを渡した編集パネルにも 60% 幅計算と既存の高さ計算が適用されることを確認する。
  - [ ] 新規作成時の幅計算が変更されないことを検証する既存テストを確認または追加する。
  - [ ] ヘッドレス UI 統合テストはユーザー指定により追加せず、修正前のリサイズイベント再現テストは省略する。
- [ ] Task: 編集時の初期幅設定を実装する
  - [ ] `CommentInlayManager.kt:33-77, 298-321` のリサイズリスナー管理を削除し、初期 `updatePanelSize` の一度だけの適用を維持する。
  - [ ] 新規・編集の両方で、パネル表示後のエディタペインリサイズにより幅を変更しない。
  - [ ] テキスト行数に応じた既存の高さ計算を維持する。
  - [ ] 軽量テストを実行して Green を確認し、必要最小限のリファクタリング後に再実行する。
- [ ] Task: 品質検証と実装コミットを行う
  - [ ] 変更範囲のテスト、`./gradlew test`、利用可能な静的解析を実行する。
  - [ ] 新規コードのテストカバレッジが 80% 以上であることを確認する。
  - [ ] 成功後に実装変更をコミットし、コミット SHA を `plan.md` の完了タスクへ記録する。
  - [ ] 計画更新を別コミットにする。
- [ ] Task: Conductor - ユーザー手動検証
  - [ ] 新規作成時の幅維持、編集時の 60% 幅、表示後の幅固定、既存の高さ動作を手順化してユーザー承認を得る。
- [ ] Task: Phase Verification & Checkpoint
  - [ ] 変更されたコードに対応するテストを確認する。
  - [ ] `./gradlew test` と利用可能な静的解析を実行する。
  - [ ] ユーザーの明示的承認後、当該フェーズで未コミットの変更がある場合にのみチェックポイントコミットを作成する。
  - [ ] チェックポイントコミットを作成した場合のみ、その SHA を計画へ記録し、計画更新を別コミットにする。